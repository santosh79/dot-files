alias gst="git status"
alias gdc="git diff --cached"
alias lp="ls -p"
alias lrt="ls -lrt"
alias la="ls -al"

alias lr='docker container run -it --rm clojure lein repl'

alias tl='tmux ls'
alias ta='tmux at -t'

alias v='vim '

export CLICOLOR=1
export LS_COLORS exfxcxdxbxegebabagacad
export TERM=xterm-256color

RED="\[\033[0;31m\]"
YELLOW="\[\033[0;33m\]"
GREEN="\[\033[0;32m\]"
NO_COLOR="\[\033[0m\]"

export PYTHONPATH=/opt/local/Library/Frameworks/Python.framework/Versions/2.6/lib/python2.6/site-packages

alias tn='tmux new -s'
alias tr='tmux-session restore'
alias ts='tmux-session save'

alias mt='mix test --exclude skip'
alias mtc='mix test --only current'
alias mts='mix test --only skip'
alias im='iex -S mix'


export PATH="/usr/local/bin:$PATH"
export PATH="$HOME/bin:/usr/local/bin:$PATH"
export PATH=$HOME/bin:$PATH
export PATH="/usr/local/opt/ncurses/bin:$PATH"
source "$HOME/.cargo/env"

# DOCKER stuff
# Container stuff
alias dcls='docker container ls -a'
alias dc='docker container '
alias dcs='docker container start -i '
alias dp='docker container run --publish '
alias dcit='docker container run -it '
alias dps='docker container ps -a '
alias dcrn='docker container run --name '
alias dcrm='docker container run rm '
alias dsh='docker container exec -it '

# Docker Image
alias di='docker image '
alias dimg='docker image ls '

# Docker Network
alias dn='docker network '
alias dnls='docker network ls'
alias dni='docker network inspect '

# create a network
alias dncr='docker network create --driver '

# attach/detach a container to a network
alias dncon='docker network connect '
alias ddcon='docker network disconnect '
alias dev='docker container run -it santoshdocker2021/dev bash'
alias mirb='docker container run -it --rm santoshdocker2021/dev irb'
alias miex='docker container run -it --rm santoshdocker2021/dev iex'
alias mnode='docker container run -it --rm santoshdocker2021/dev node'


# building docker from Dockerfile
alias dbt='docker image build -t '
# Example: `dbb <some_tag> .` -- builds into current dir
# where Dockerfile is located
